# Prompt — benchmark-derived layout grammar

Annotate only the final selected benchmark figures. Record normalized panel rectangles,
roles, chart types, topology, anchor share, workflow share, white-space, legend strategy,
direct labeling, and text density. Aggregate with relevance weighting. Combine the resulting
soft prior with v2.2 contribution weights. Never copy exact benchmark artwork or exact geometry.
